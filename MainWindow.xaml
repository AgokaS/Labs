<Window x:Class="Karay1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"      
        mc:Ignorable="d"
        Background="{x:Null}"
        Title="MainWindow" 
        Height="595.039" 
        Width="900.459"
        Foreground="{x:Null}" WindowStyle="None" AllowsTransparency="True">
    <Grid Margin="10,0,2,18">
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid
            x:Name="StudGrid" 
            HorizontalAlignment="Left" 
            Width="844" Margin="10,80,0,10" Foreground="Black" Background="{x:Null}"/>
        <Button
            x:Name="Buton_Close" 
            Content="Close"
            Margin="10,10,0,520" 
            RenderTransformOrigin="18.302,5.64" 
            Click="Buton_Close_Click" 
            HorizontalAlignment="Left" 
            Width="125" Foreground="Black" />
    </Grid>
</Window>
